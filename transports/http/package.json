{
    "name": "@repo/transports/http",
    "version": "1.0.0",
    "description": "HTTP transport for weather SDK",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "jest"
    },
    "dependencies": {
        "@repo/core": "workspace:*"
    },
    "devDependencies": {
    }
}